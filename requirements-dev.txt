-r requirements-pipeline.txt
# The API is tested too, so the dev environment needs what it runs on.
#
# This is not a hole in the separation: the SERVING image installs
# requirements-serve.txt alone, and tests assert both that the file excludes
# the HTTP client and that the Dockerfile installs nothing else. Omitting this
# line is what made CI fail with ModuleNotFoundError on fastapi while the local
# environment passed — because the local one had it installed by hand.
-r requirements-serve.txt

pytest==8.4.1
pyyaml==6.0.2
respx==0.22.0
ruff==0.12.5
mypy==1.17.0

# Used only by scripts/power_calculation.py, which must stay reproducible.
numpy==2.5.2
scikit-learn==1.9.0
